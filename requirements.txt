# Dependencias del pipeline de prediccion de zonas PTM (pipeline.py).
pandas>=1.5.0
numpy>=1.22.0

# Fase 1.5 (estructura, PDB/mmCIF): parseo 100% local via gemmi (sin llamadas
# de red). Libreria liviana, sin conflictos de dependencias con el resto del
# pipeline.
gemmi>=0.6.0

# NOTA: DeepMVP y DeepPTMPred (Fase 2, motores de prediccion de PTMs) NO se
# instalan aqui -- cada uno vive en su propio venv/conda env dedicado
# (DeepMVP: conda env 'deepmvp', TensorFlow 2.4.x/Python 3.7.10; DeepPTMPred:
# conda env 'deepptmpred', TensorFlow 2.15 + PyRosetta + checkpoint ESM-2),
# mismo patron que los motores de B-Cell-Epitope-Prediction. Los wrappers
# (src/engines/deepmvp_engine.py, deepptmpred_engine.py) SI estan construidos
# e invocan esos entornos por subprocess -- ver STATUS.md para el estado real
# de instalacion en esta maquina.

pytest>=7.0.0
